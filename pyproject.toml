[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "datasetlint"
version = "0.1.0"
description = "Lightweight validation for robotics and Physical AI datasets."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "DatasetLint contributors" }
]
keywords = ["robotics", "datasets", "validation", "physical-ai", "lint"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering",
]
dependencies = [
  "numpy>=1.23",
  "pandas>=1.5",
  "pydantic>=2",
  "typer>=0.9",
  "rich>=13",
]

[project.optional-dependencies]
dev = [
  "numpy<2.5",
  "pytest>=7",
  "ruff>=0.4",
  "mypy>=1.8",
]

[project.scripts]
datasetlint = "datasetlint.cli:app"

[project.urls]
Homepage = "https://github.com/gagandeepreehal/datasetlint"
Source = "https://github.com/gagandeepreehal/datasetlint"
"Bug Tracker" = "https://github.com/gagandeepreehal/datasetlint/issues"

[tool.setuptools.packages.find]
include = ["datasetlint*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.10"
packages = ["datasetlint"]
strict = true
disallow_untyped_decorators = false
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = ["pandas", "pandas.*", "rich", "rich.*", "typer", "typer.*"]
ignore_missing_imports = true
